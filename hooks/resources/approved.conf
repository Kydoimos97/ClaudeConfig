# approved.commands — entries auto-approved by command-guard
#
# Two entry types, matched by context:
#   Bash command prefixes  — segment equals prefix or starts with "prefix "
#   Claude Code tool names — exact match against tool_name (PascalCase)
#
# Lines starting with # and blank lines are ignored.

# ── Claude Code tools ─────────────────────────────────────────────────────────
Read
Edit
Glob
Grep

# ── Filesystem / shell utils ──────────────────────────────────────────────────
cd
ls
pwd
tree
find
rg
grep
xargs
wc
cat
head
tail
echo
printf
sed
awk
sort
uniq
cut
tee
date
jq
yq
mktemp
which
where
type
file
stat
du
df
env
printenv
test
true
false
diff
tr
column
touch
basename
dirname
realpath
readlink
open
cp
mv
mkdir

# ── Git (read-only) ───────────────────────────────────────────────────────────
git status
git diff
git show
git log
git branch
git checkout
git tag
git rev-parse
git blame
git worktree
git stash list
git ls-files
git remote
git fetch
git describe
git shortlog
git reflog

# ── Python / uv ───────────────────────────────────────────────────────────────
uv run
uv sync
uv pip
uv lock
uv tree
uvx

# ── Task runner ───────────────────────────────────────────────────────────────
task

# ── GitHub CLI ────────────────────────────────────────────────────────────────
gh pr
gh repo
gh api
gh run
gh auth
gh issue
gh release

# ── Node ──────────────────────────────────────────────────────────────────────
npm run
npm test
pnpm
npx tsc
npx vite

# ── Terraform ─────────────────────────────────────────────────────────────────
terraform plan
terraform validate
terraform workspace
terraform init

# ── Docker ────────────────────────────────────────────────────────────────────
docker compose logs
docker compose ps
docker compose build
docker build
docker image
docker images
docker ps
docker inspect

# ── Database ──────────────────────────────────────────────────────────────────
psql

# ── Custom / agents ───────────────────────────────────────────────────────────
puppy
kiro
kiro-cli
codex
curl
bash

# ── Python (direct script execution) ─────────────────────────────────────────
# python -m and pip variants are blocked — see blocked.commands
python
python3